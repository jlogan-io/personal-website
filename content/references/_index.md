---
title: References
# This section has no page of its own -- its entries render inside
# /experience/, which carries the /references/ alias.
build:
  render: never
  list: never
cascade:
  - build:
      render: never
      list: local
    target:
      path: '/references/**'
---

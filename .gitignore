# Hugo build output. Never commit this: the copy on bravo is a `hugo server`
# dev build carrying livereload.js, and publishing it would break the live site.
/public/
/resources/
.hugo_build.lock

# macOS
.DS_Store

# Fonts fetched on demand by scripts/make-og-card.py
/.cache/
